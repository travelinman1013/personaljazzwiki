{
  "name": "personal-artist-wiki",
  "type": "module",
  "version": "1.0.0",
  "description": "A personal wiki for 2500+ musician profiles with semantic search",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "./scripts/sync-content.sh",
    "build:prod": "npm run sync && npm run build && npm run postbuild",
    "postbuild": "npx pagefind --site dist"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.16.6",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.1",
    "remark-wiki-link": "^2.0.1",
    "tailwindcss": "^3.4.19"
  },
  "devDependencies": {
    "@types/node": "^25.0.3"
  }
}
